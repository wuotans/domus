"use client";import {useEffect,useState} from "react";import {AppShell} from "@/components/AppShell";
export default function Page(){const [items,setItems]=useState<any[]>([]);const [name,setName]=useState("");const [type,setType]=useState("EXPENSE");const load=()=>fetch("/api/categories").then(r=>r.json()).then(setItems);useEffect(()=>{ void load(); },[]);
async function add(e:any){e.preventDefault();await fetch("/api/categories",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,type})});setName("");load()}
return <AppShell title="Categorias"><div className="grid2"><form className="panel" onSubmit={add}><h2 className="section-title">Nova categoria</h2><div className="field"><label>Nome</label><input value={name} onChange={e=>setName(e.target.value)} required/></div><div className="field" style={{marginTop:12}}><label>Tipo</label><select value={type} onChange={e=>setType(e.target.value)}><option value="EXPENSE">Despesa</option><option value="INCOME">Receita</option></select></div><button className="btn" style={{marginTop:14}}>Cadastrar</button></form><div className="panel"><h2 className="section-title">Categorias</h2><div className="table-wrap"><table className="table"><tbody>{items.map(x=><tr key={x.id}><td>{x.name}</td><td><span className="badge">{x.type==="INCOME"?"Receita":"Despesa"}</span></td></tr>)}</tbody></table></div></div></div></AppShell>}