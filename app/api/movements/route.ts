import { prisma } from "@/lib/prisma";import { NextResponse } from "next/server";import { getServerSession } from "next-auth";import { authOptions } from "@/lib/auth";
export async function GET(){return NextResponse.json(await prisma.movement.findMany({include:{category:true,user:true},orderBy:{date:"desc"}}))}
export async function POST(req:Request){const session=await getServerSession(authOptions);if(!session?.user?.id)return NextResponse.json({error:"Não autorizado"},{status:401});const b=await req.json();const item=await prisma.movement.create({data:{type:b.type,description:b.description,amount:Number(b.amount),date:new Date(b.date),categoryId:b.categoryId,userId:session.user.id,reference:b.reference||null,notes:b.notes||null}});return NextResponse.json(item,{status:201})}
