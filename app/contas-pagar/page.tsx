"use client";import {useEffect,useState} from "react";import {AppShell} from "@/components/AppShell";import {Modal} from "@/components/Modal";
export default function Page(){const[open,setOpen]=useState(false);const [items,setItems]=useState<any[]>([]),[cats,setCats]=useState<any[]>([]);const [f,setF]=useState<any>({description:"",supplier:"",amount:"",dueDate:"",categoryId:"",notes:""});const load=()=>Promise.all([fetch("/api/payables").then(r=>r.json()).then(setItems),fetch("/api/categories").then(r=>r.json()).then((x:any[])=>setCats(x.filter(c=>c.type==="EXPENSE"))) ]);useEffect(()=>{load()},[]);
async function add(e:any){e.preventDefault();await fetch("/api/payables",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});setF({description:"",supplier:"",amount:"",dueDate:"",categoryId:"",notes:""});load()}
return <AppShell title="Contas a pagar"><div className="page-actions"><button className="btn" onClick={()=>setOpen(true)}>+ Novo cadastro</button></div><Modal open={open} title="Novo cadastro" onClose={()=>setOpen(false)}><form className="panel form" onSubmit={add}><div className="field"><label>Descrição</label><input value={f.description} onChange={e=>setF({...f,description:e.target.value})} required/></div><div className="field"><label>Fornecedor</label><input value={f.supplier} onChange={e=>setF({...f,supplier:e.target.value})}/></div><div className="field"><label>Valor</label><input type="number" step="0.01" value={f.amount} onChange={e=>setF({...f,amount:e.target.value})} required/></div><div className="field"><label>Vencimento</label><input type="date" value={f.dueDate} onChange={e=>setF({...f,dueDate:e.target.value})} required/></div><div className="field"><label>Categoria</label><select value={f.categoryId} onChange={e=>setF({...f,categoryId:e.target.value})} required><option value="">Selecione</option>{cats.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="field"><label>Observações</label><input value={f.notes} onChange={e=>setF({...f,notes:e.target.value})}/></div><div><button className="btn">Cadastrar conta</button></div></form></Modal><div className="panel" style={{marginTop:16}}><div className="table-wrap"><table className="table"><thead><tr><th>Descrição</th><th>Fornecedor</th><th>Categoria</th><th>Vencimento</th><th>Valor</th><th>Status</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.description}</td><td>{x.supplier||"-"}</td><td>{x.category?.name}</td><td>{new Date(x.dueDate).toLocaleDateString("pt-BR")}</td><td>{Number(x.amount).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}</td><td><span className="badge">{x.status}</span></td></tr>)}</tbody></table></div></div></AppShell>}