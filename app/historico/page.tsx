import {AppShell} from "@/components/AppShell";import {prisma} from "@/lib/prisma";
export default async function Page(){const logs=await prisma.auditLog.findMany({include:{user:true},orderBy:{createdAt:"desc"},take:200});const mov=await prisma.movement.findMany({include:{user:true,category:true},orderBy:{createdAt:"desc"},take:200});return <AppShell title="Histórico de movimentações"><div className="panel"><h2 className="section-title">Movimentações financeiras</h2><div className="table-wrap"><table className="table"><thead><tr><th>Data</th><th>Usuário</th><th>Ação</th><th>Descrição</th><th>Valor</th></tr></thead><tbody>{mov.map(x=><tr key={x.id}><td>{x.createdAt.toLocaleString("pt-BR")}</td><td>{x.user.name}</td><td>{x.type==="INCOME"?"Entrada":"Saída"}</td><td>{x.description}</td><td>{Number(x.amount).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}</td></tr>)}</tbody></table></div></div>{logs.length>0&&<div className="panel" style={{marginTop:16}}><h2 className="section-title">Auditoria</h2>{logs.map(x=><p key={x.id}>{x.createdAt.toLocaleString("pt-BR")} · {x.action} · {x.entity}</p>)}</div>}</AppShell>}